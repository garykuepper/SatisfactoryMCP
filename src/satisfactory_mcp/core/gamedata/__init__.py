"""The game's own data: items, recipes, buildings, schematics.

Everything here is derived from the shipped ``Docs.json`` and knows nothing about
a save, a plan or a response. It was ``satisfactory_mcp.docs``, which read like the
repo's own prose directory; that name survives as an alias shim.
"""
